import { darkColors, lightColors } from "../../theme/colors";
import { NavTheme, TopbarTheme } from "./types";

export const lightTopbar: TopbarTheme = {
  background: lightColors.backgroundTopbar,
};

export const darkTopbar: TopbarTheme = {
  background: darkColors.backgroundTopbar,
};

export const light: NavTheme = {
  background: lightColors.backgroundAlt,
};

export const dark: NavTheme = {
  background: darkColors.backgroundAlt,
};
